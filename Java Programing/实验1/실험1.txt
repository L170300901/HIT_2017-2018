실험 1:자바 기본 프로세스 설계
1. 실험 목적
1)표준 입력 수출 함수의 사용을 입력합니다.
2)정적 함수의 정의와 사용(본 실험 요구 모든 함수는 모두 정지 함수)입니다.
2)간단한 정렬 알고리즘
3)자바 기반 문법 종합 운행(비접시 대상 BMI프로그램)
이항 실험 내용
1)BMI를 본으로 하여 Main함수에 수 세트 String, String, String, Float, float, float, Flis, Flos,상기된 수치는 두개의 작은 저장 용량을 보유하고 있어야 한다.
2)inputstudents함수를 상기하며 이 함수의 파라미터는 여러 학생의 관련 정보를 입력하여 해당 데이터를 해당 데이터에 저장하고 해당 데이터를 해당 데이터에 저장합니다. 
3)BMI류에서는 함수의 일종인 checkHealth, 함수의 파라미터를 bmi값으로 표시하고, 이 함수는 아래 표에서 BMI추출물을 판단하고, 이 함수의 귀착점은 다음과 같습니다.

4)BMI에서는 5개의 정렬한 SortByXXX함수를 추가로 늘려 학생 번호, 성명, 키, 몸무게, BMI를 작게 배열하고 정렬 정렬한 알고리즘을 활용하여 정렬한 알고리즘을 활용하면 된다.정렬한 경우에는 동일한 학생이 동일한 그룹에서 동일한 번호에 해당하는 경우에는 반드시 동일한 기준을 적용해야 합니다!이를 실현하기 위해 배열된 인덱스의 인덱스는 배열된 배열 순서로 배열되어 배열된 배열이 끝난 뒤 배열된 배열을 토대로 배열된 배열이다.
5)BMI에서는 BrintStudents함수를 추가하고 이 함수의 파라미터는 int sortedindex를 인쇄할 수 있습니다.인쇄를 할 때마다 학생들의 정보가 인쇄되어 있고, 명확하게 배우기 위해 호, 이름, 키, 몸무게, 계산한 bmi값을 떼어 낸다.
6)menu함수를 이용하여 학생, 학생, 학생, 5종의 순서, 프로그램 퇴출 등 8종의 옵션을 제공하며, 가입자가 지정한 옵션을 입력하여 해당 함수의 기능을 실행합니다.inputstudents함수를 호출하기 전에 가입자 수를 입력하여 인수 인원 수를 입력하도록 한다.
7)BMI main함수에서는 menu함수를 호출하여 각 방면의 기능을 테스트한다.
키, 몸무게, bmi등의 수치는 두개의 작은 사이즈의 양식을 유지하고 저장하는 것이 특징입니다.
3. 실험 코드
주의:프로그램 코드와 운행 결과를 게시하고 주의 소스에 바코드를 붙이지 않은 1/3, 프로그래밍 소스의 1/3에 해당하는 소스 코드는 줄이고, 압축 파일은 학호에 따라 다른 번호로 나눠 주세요.